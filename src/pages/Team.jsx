/**
 * Team page (/team route)
 * Renders the full TeamSection component — single source of truth for team cards.
 * The PageLayout wrapper provides the page title, navbar offset, and consistent padding.
 */
import TeamSection from '../components/TeamSection';

export default function Team() {
  return <TeamSection />;
}

